ShopMate DOM vanilla js app. 

The DOM is created by the browser when a web page is loaded and uses parent/child structure relations. In graphical form, it represent a tree of elements / nodes. With this shopping list app i have focused on interacting with the DOM, DOM travesal, Nodes, Events, Forms and manipulation and layout of html elements in a particular way that makes the code more reusable and scalable. 





